{
    "builds": [{
        "src": "expenseswebsite/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.11"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "expenseswebsite/wsgi.py"
        }
    ],
    "installCommand": "apt-get update && apt-get install -y libcairo2 libpango-1.0-0 libgdk-pixbuf2.0-0 libffi-dev && pip install -r requirements.txt"
}